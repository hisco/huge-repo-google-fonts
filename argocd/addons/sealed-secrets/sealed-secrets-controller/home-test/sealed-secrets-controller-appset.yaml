###########################################################################
# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
# 
# This ApplicationSet deploys the sealed-secrets-controller Helm component to the home-test cluster.
# It uses a git file generator to read cluster configuration and deploy
# the Helm chart with cluster-specific values.
###########################################################################
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: sealed-secrets-controller-home-test
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/hisco/huge-repo-google-fonts
        revision: HEAD
        files:
          - path: argocd/cluster-config/home-test/config.yaml
  template:
    metadata:
      name: sealed-secrets-controller-{{ .cluster.name }}
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: "{{ .addons.argocdProject }}"
      source:
        repoURL: https://github.com/hisco/huge-repo-google-fonts
        targetRevision: "{{ .addons.targetRevision }}"
        path: argocd/addons/sealed-secrets/sealed-secrets-controller/{{ .addons.overlay }}
        helm:
          releaseName: sealed-secrets-controller
          valueFiles:
            - ../values.yaml
            - values.yaml
      destination:
        server: "{{ .cluster.address }}"
        namespace: sealed-secrets
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
